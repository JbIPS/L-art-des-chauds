backend:
  name: git-gateway
  #repo: JbIPS/L-art-des-chauds
  branch: master

media_folder: "images/uploads" # Media files will be stored in the repo under images/uploads

collections:
  - name: "products"
    label: "Produits"
    folder: "_products"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Nom", name: "name", widget: "string"}
